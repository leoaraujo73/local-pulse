services:
  web:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: python -m http.server 3000 --bind 0.0.0.0
    ports:
      - "3000:3000"
    restart: unless-stopped
